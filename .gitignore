# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bulk source data (drug databases, imports). These are working files: they are
# uploaded through the admin importer, not served to browsers. 67 MB of them
# were committed under public/ and shipped to every visitor as a public
# download that nothing in the application referenced.
public/data/
